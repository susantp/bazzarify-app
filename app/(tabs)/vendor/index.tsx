import { SafeAreaWrapper } from "@/components/common/SafeAreaWrapper";
import ScreenHeader from "@/components/common/ScreenHeader";

export default function Page() {
  return (
    <SafeAreaWrapper>
      <ScreenHeader title="Vendor Home" />
    </SafeAreaWrapper>
  );
}
